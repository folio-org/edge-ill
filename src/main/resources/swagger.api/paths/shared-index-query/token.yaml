post:
  tags: [ SharedIndexQuery ]
  description: "Retrieve a token from the Shared Index authentication server"
  operationId: sharedIndexQueryToken
  requestBody:
    required: false
    content:
      application/json:
        schema:
          type: object
  responses:
    '200':
      description: Successful response
      content:
        application/json: {}
    '400':
      $ref: '../../error-responses/BadRequest.yaml'
    '500':
      $ref: '../../error-responses/InternalServerError.yaml'
