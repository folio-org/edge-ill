get:
  tags: [ External ]
  description: "Generates a brief status report about the service"
  operationId: statusReport
  parameters:
    - $ref: '../../parameters/path/tenant-id.yaml'
  responses:
    '200':
      description: Successful response
      content:
        application/json:
          schema:
            type: object
    '400':
      $ref: '../../error-responses/BadRequest.yaml'
    '500':
      $ref: '../../error-responses/InternalServerError.yaml'
