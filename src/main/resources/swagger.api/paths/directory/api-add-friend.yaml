get:
  tags: [ Directory ]
  description: "Fetches the specified url and adds the found institutions as directory entries"
  operationId: directoryApiAddFriend
  responses:
    '200':
      description: Successful response
      content:
        application/json: {}
    '400':
      $ref: '../../error-responses/BadRequest.yaml'
    '500':
      $ref: '../../error-responses/InternalServerError.yaml'
