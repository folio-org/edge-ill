get:
  tags: [ Timers ]
  description: "Executes the timer with the supplied code"
  operationId: timersExecute
  responses:
    '200':
      description: Successful response
      content:
        application/json: {}
    '400':
      $ref: '../../error-responses/BadRequest.yaml'
    '500':
      $ref: '../../error-responses/InternalServerError.yaml'
