get:
  tags: [ PatronRequests ]
  description: "Actions the requests in the batch as printed"
  operationId: markBatchAsPrinted
  responses:
    '200':
      description: Successful response
      content:
        application/json: {}
    '400':
      $ref: '../../error-responses/BadRequest.yaml'
    '500':
      $ref: '../../error-responses/InternalServerError.yaml'
