get:
  tags: [ TestHostLMS ]
  description: "Validates the user against the specified Host LMS"
  operationId: testHostLMSValidate
  responses:
    '200':
      description: Successful response
      content:
        application/json: {}
    '400':
      $ref: '../../error-responses/BadRequest.yaml'
    '500':
      $ref: '../../error-responses/InternalServerError.yaml'
